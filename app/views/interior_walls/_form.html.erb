<%= form_for(@interior_wall) do |f| %>
  <% if @interior_wall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interior_wall.errors.count, "error") %> prohibited this interior_wall from being saved:</h2>

      <ul>
      <% @interior_wall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
