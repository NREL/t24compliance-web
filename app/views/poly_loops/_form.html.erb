<%= form_for(@poly_loop) do |f| %>
  <% if @poly_loop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poly_loop.errors.count, "error") %> prohibited this poly_loop from being saved:</h2>

      <ul>
      <% @poly_loop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
