<%= form_for(@curve_cubic) do |f| %>
  <% if @curve_cubic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curve_cubic.errors.count, "error") %> prohibited this curve_cubic from being saved:</h2>

      <ul>
      <% @curve_cubic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
