<h1>Listing ceilings</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Area</th>
      <th>Adjacent space reference</th>
      <th>Construct assembly reference</th>
      <th>Exterior solar absorptance</th>
      <th>Exterior thermal absorptance</th>
      <th>Exterior visible absorptance</th>
      <th>Interior solar absorptance</th>
      <th>Interior thermal absorptance</th>
      <th>Interior visible absorptance</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ceilings.each do |ceiling| %>
      <tr>
        <td><%= ceiling.name %></td>
        <td><%= ceiling.area %></td>
        <td><%= ceiling.adjacent_space_reference %></td>
        <td><%= ceiling.construct_assembly_reference %></td>
        <td><%= ceiling.exterior_solar_absorptance %></td>
        <td><%= ceiling.exterior_thermal_absorptance %></td>
        <td><%= ceiling.exterior_visible_absorptance %></td>
        <td><%= ceiling.interior_solar_absorptance %></td>
        <td><%= ceiling.interior_thermal_absorptance %></td>
        <td><%= ceiling.interior_visible_absorptance %></td>
        <td><%= link_to 'Show', ceiling %></td>
        <td><%= link_to 'Edit', edit_ceiling_path(ceiling) %></td>
        <td><%= link_to 'Destroy', ceiling, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ceiling', new_ceiling_path %>
