<%= form_for(@curve_linear) do |f| %>
  <% if @curve_linear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curve_linear.errors.count, "error") %> prohibited this curve_linear from being saved:</h2>

      <ul>
      <% @curve_linear.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
